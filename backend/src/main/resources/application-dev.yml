# Development profile — verbose logging, dev-friendly settings
logging:
  level:
    com.sadbob.dentalclinic: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

spring:
  jpa:
    show-sql: true